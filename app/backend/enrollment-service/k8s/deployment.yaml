apiVersion: apps/v1
kind: Deployment
metadata:
  name: enrollment-service
  namespace: cloudblitz
  labels:
    app: enrollment-service
    version: v1
    tags.datadoghq.com/env: dev
    tags.datadoghq.com/service: enrollment-service
    tags.datadoghq.com/version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: enrollment-service
  template:
    metadata:
      labels:
        app: enrollment-service
        version: v1
        tags.datadoghq.com/env: dev
        tags.datadoghq.com/service: enrollment-service
        tags.datadoghq.com/version: v1
        admission.datadoghq.com/enabled: "true"
      annotations:
        admission.datadoghq.com/java-lib.version: v1.53.0
    spec:
      containers:
      - name: enrollment-service
        image: cloudblitz/enrollment-service:latest
        ports:
        - containerPort: 8083
        env:
        - name: MONGO_URI
          value: "mongodb+srv://atulyw:R8RfzxlNDb6Z68pe@cdec-dev.et2yiaw.mongodb.net/cb_enrollments?retryWrites=true&w=majority&appName=cdec-dev"
        - name: JWT_SECRET
          value: "CloudBlitzJWTSecretKeyForSecureAuthentication2024!@#"
        - name: SERVER_PORT
          value: "8083"
        - name: DD_LOGS_INJECTION
          value: "true"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/enroll/health
            port: 8083
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/enroll/health
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 10
        imagePullPolicy: Always
      restartPolicy: Always

